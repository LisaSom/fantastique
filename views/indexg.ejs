<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Licornes</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<body>
    <section>
        
        <h2>voitures : </h2>
        <div class="ajout">
            <% resultat.forEach( (r)=> { %>
                <input type="submit" class="voitures" name="<%= r.nom %>" value="<%= r.nom %>">
                <div id="<%= r._id %>"></div>
            <% }); %>
        </div>
    </section>

    <script>
        let licorne = document.getElementsByClassName("voitures");
        for(let v of voitures){
            l.addEventListener("click", (event) =>{
                axios.get("/voitures/"+v.getAttribute('nom')).then(response => {
                    document.getElementById(response.data._id).innerHTML = '<p> nom : '+ response.data.nom +'</p>'
                    })       
            })
        }
    </script>
    
</body>
</html>
